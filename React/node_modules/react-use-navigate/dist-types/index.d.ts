import { FC } from 'react';
declare type UseNavigateProps = {
    when?: boolean;
    goTo?: string;
    onPaths?: string[];
    notOnPaths?: string[];
    otherwiseGoTo?: string;
};
declare type UseNavigateConfig = {
    push: (path: string) => void;
    back: () => void;
    replace: (path: string) => void;
};
export declare const NavigateProvider: FC<UseNavigateConfig>;
declare const useNavigate: () => {
    push: ({ goTo, otherwiseGoTo, when, onPaths, notOnPaths, }: UseNavigateProps) => void;
    back: ({ goTo, otherwiseGoTo, when, onPaths, notOnPaths, }: UseNavigateProps) => void;
    replace: ({ goTo, otherwiseGoTo, when, onPaths, notOnPaths, }: UseNavigateProps) => void;
};
export default useNavigate;
